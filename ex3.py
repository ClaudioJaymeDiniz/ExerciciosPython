from random import sample
a = sample(range(100), 10)
b = sample(range(100), 10)
merge = []

for x in range 20:
    if x % 2 =! 0:
        merge.append(numeros[x])
    else:
        impares.append(numeros[x])
